{
  "name": "party",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "partykit dev",
    "deploy": "partykit deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@liminal/shared": "workspace:*"
  },
  "devDependencies": {
    "partykit": "^0.0.115",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
